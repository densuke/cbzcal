# On Task Completion

## Verification
1. Run `cargo fmt` to ensure code is formatted.
2. Run `cargo clippy` to check for common mistakes and best practices.
3. Run `cargo test` to ensure all tests pass.
4. Verify with manual CLI runs if necessary (e.g., `cargo run -- doctor`).

## Documentation
- Update `docs/` if architecture or development flow changes.
